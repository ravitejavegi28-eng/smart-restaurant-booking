{
  "name": "smart-restaurant-booking",
  "version": "1.0.0",
  "private": true,
  "description": "Responsive restaurant table booking with a secure staff dashboard.",
  "scripts": {
    "local": "node scripts/dev.js",
    "test": "node --test",
    "check": "node --check scripts/dev.js && node --check api/bookings.js && node --check api/staff/login.js && node --check api/staff/bookings.js && node --check public/assets/js/booking.js && node --check public/assets/js/staff.js"
  },
  "engines": {
    "node": ">=20"
  }
}
